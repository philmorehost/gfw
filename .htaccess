RewriteEngine On

# Redirect all requests to root index.php unless it's a real file
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route to root index.php
RewriteRule ^(.*)$ index.php [L,QSA]
